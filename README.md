# Falconer bug bash docs (throwaway branch)

This orphan branch exists only for a Falconer GitHub two-way sync bug bash. It shares no history with `main` and nothing here affects the rest of the repo. Delete it with `git push origin --delete falconer-bugbash` when the bug bash is over.